# Real inventory & config may contain site data — commit only the .example files.
config/servers.csv
config/servers.csv.bak
config/servers.deploy.csv
config/*.local.psd1

# InfraServerSetup dashboard: generated demo data (recreate with tools\New-SampleData.ps1)
InfraServerSetup/sample/

# Run logs
logs/*.csv
logs/*.log
!logs/.gitkeep

# Answer files with injected secrets (root passwords, crypted hashes)
templates/*.local.*
*.rendered

# Site-specific syslog CA certs (env-specific; provide your own)
config/*.pem
config/*.crt
platforms/dell/idrac_list.csv

# Editor / OS cruft
.vscode/
.idea/
Thumbs.db
*.tmp
